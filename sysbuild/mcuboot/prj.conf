#
# This is a copy of zephyr/bootloader/mcuboot/boot/zephyr/prj.conf
# Required in order to have mcuboot board overlays in the sysbuild/mcuboot
# subdirectory. It must be kept in synch with the zephyr/bootloader/mcuboot
# tree.
#
# See:
# https://github.com/zephyrproject-rtos/zephyr/discussions/63067
#
# Please clearly describe any changes made to this file.
#
# XXX: there should be a setup script that copies this and adds wallabmc changes.
#
CONFIG_BOOT_SIGNATURE_TYPE_NONE=y

CONFIG_PM=n

CONFIG_MAIN_STACK_SIZE=10240

CONFIG_BOOT_SWAP_SAVE_ENCTLV=n
CONFIG_BOOT_ENCRYPT_IMAGE=n

CONFIG_BOOT_UPGRADE_ONLY=n
CONFIG_BOOT_BOOTSTRAP=n

### mbedTLS has its own heap
# CONFIG_HEAP_MEM_POOL_SIZE is not set

CONFIG_FLASH=y

### Various Zephyr boards enable features that we don't want.
# CONFIG_BT is not set
# CONFIG_I2C is not set

CONFIG_LOG=y
CONFIG_LOG_MODE_MINIMAL=y
### Ensure Zephyr logging changes don't use more resources
CONFIG_LOG_DEFAULT_LEVEL=0
### Use info log level by default
CONFIG_MCUBOOT_LOG_LEVEL_INF=y
### Decrease footprint by ~4 KB in comparison to CBPRINTF_COMPLETE=y
CONFIG_CBPRINTF_NANO=y
### Use picolibc to reduce flash usage
CONFIG_PICOLIBC=y
### Disable malloc arena because we don't need it
CONFIG_COMMON_LIBC_MALLOC_ARENA_SIZE=0
